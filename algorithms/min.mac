! Выборм min из двух чисел
! имя:
!    *use,algorithms/min.mac,ARG1,ARG2
!
! входные данные, являющиеся локальными переменными:
!    ARG1 - первое число
!    ARG2 - второе число
!
! выходные данные:
!    _RETURN -  min из двух чисел
_RETURN = ARG1 < ARG2