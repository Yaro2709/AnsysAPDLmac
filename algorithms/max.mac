! Выборм max из двух чисел
! имя:
!    *use,algorithms/max.mac,ARG1,ARG2
!
! входные данные, являющиеся локальными переменными:
!    ARG1 - первое число
!    ARG2 - второе число
!
! выходные данные:
!    _RETURN -  max из двух чисел
_RETURN = ARG1 > ARG2